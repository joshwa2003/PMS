import React from 'react';
import { Box } from '@mui/material';
import MDBox from 'components/MDBox';

const JobMonitoringFilters = ({ filters, onFilterChange }) => {
  return (
    <MDBox>
      <Box>Job Monitoring Filters Component</Box>
    </MDBox>
  );
};

export default JobMonitoringFilters;
