import React from 'react';
import { Box } from '@mui/material';
import MDBox from 'components/MDBox';

const JobFilters = ({ filters, onFilterChange }) => {
  return (
    <MDBox>
      <Box>Job Filters Component</Box>
    </MDBox>
  );
};

export default JobFilters;
